import React from 'react';
import { Card } from 'antd';

const ScheduleTest: React.FC = () => {
  return (
    <div>
      <Card title="排班管理测试页面">
        <p>如果您能看到这个页面，说明路由配置是正确的。</p>
        <p>问题可能出现在组件的导入或API调用上。</p>
      </Card>
    </div>
  );
};

export default ScheduleTest;
